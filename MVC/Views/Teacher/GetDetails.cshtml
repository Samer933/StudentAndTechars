@model IEnumerable<MVC.Models.Teacher>

@{
    ViewBag.Title = "GetDetails";
}

@Styles.Render("~/TeacherDesign")


<table class="table">
    <tr class="compenent">
        <th>
            @Html.DisplayName("Teacher Name")
            @*@Html.DisplayNameFor(model => model.teacherName)*@
        </th>
        <th>
            @Html.DisplayName("Teacher Number")
            @*@Html.DisplayNameFor(model => model.teacherNo)*@
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr class="compenent">
        <td>
            @Html.DisplayFor(modelItem => item.teacherName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.teacherNo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.teacherId } , new {id = "id" })

            @*@Html.ActionLink("Home", "Index", Nothing, New With {.style = "font-weight:bold;", .class = "someClass"})*@

            @*@Html.ActionLink("Delete", "Delete", new { id=item.teacherId })*@
            <a class="link" href="/Teacher/Delete/@item.teacherId">Delete</a>
        </td>
    </tr>
}

</table>
